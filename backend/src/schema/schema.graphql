type Query {
  helloWorld: String!
  userRepositories(queryString: String): Any
}

type UserDetails {
  name: String!
  imageUrl: String!
}

type Repositories {
  user: String!
  repositories: [Repository]
}

type Repository {
  name: String!
}
