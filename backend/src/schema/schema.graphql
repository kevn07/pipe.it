type Query {
  helloWorld: String!
  queryRepositories(queryString: String): Repositories
}

type UserDetails {
  name: String!
  imageUrl: String!
}

type Repositories {
  user: String
  repositories: [Repository]
}

type Repository {
  name: String!
}
